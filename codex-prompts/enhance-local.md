---
description: 调用本地提示词增强器改写草稿（与 WorkBuddy 同款模板与模型）
argument-hint: DRAFT="你的草稿"
---

请只做一件事：通过终端执行下面这条命令，然后把命令的标准输出原文发给我。

命令：
python "<你克隆下来的路径>\prompt-enhancer\cli.py" "$DRAFT"

其中 $DRAFT 的值是：

$DRAFT

要求：
1. 不要自己改写这段草稿，改写由那个脚本调用独立模型完成。
2. 如果命令失败（退出码非 0 或有 [warn] 输出），把错误信息原样告诉我，并把运行命令时打印的
   标准输出（即便它是降级后的原文）一并贴出来，然后简短说明增强服务当前不可用。
3. 把脚本输出作为你回复的第一段，不要加"命令输出如下"之类的引导语，也不要重复粘贴两次。
4. 不要修改这个脚本，也不要顺手做其他改动。
