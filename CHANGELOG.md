# 更新日志

版本号以 `VERSION` 为准。以下记录面向用户的变化和发行流程调整。

## [1.1.0] - 2026-09-24

### 改善

- 设置页面支持滚动和小屏幕布局，固定底部操作栏，增加未保存标记、关闭确认和 Ctrl+S 保存。
- 历史详情只读、保留选中记录，空历史和无可复制内容时禁用对应操作。
- 严格校验数值、模型地址、提示词模板和重复快捷键；连接测试异常后可重新测试。
- 补齐 GitHub 持续集成、问题模板、贡献说明和版本发布流程。
- Windows 发行包包含使用说明、更新日志、构建信息与 SHA256 校验文件。

### 修复

- 等待增强结果时切换窗口或操作键鼠，改为仅复制结果；保留用户期间复制的新内容。
- 未自动粘贴的结果不再注入原生撤销；用户可通过剪贴板恢复原文。
- 配置保存失败时不更新内存设置；API Key 加密失败时报告错误，不再退回明文保存。
- 加强输入、配置和 API 返回值的校验，减少异常响应导致的错误行为。
- 修复完整 API 地址重复拼接、显式空密钥错误回落、零温度被默认值覆盖，以及损坏配置导致启动失败。
- 修复代理 SSE 响应延迟、版本路径重复及 HTTP 请求长度校验，完善 MCP 非法输入恢复与 Windows 中文编码。
- 打包校验版本、源文件和可执行文件的 SHA256，阻止把旧构建标成新版本。
- 启动或退出自检失败时终止发行流程，并清理临时自检目录。

### 发行说明

- 桌面版支持 Windows 10 / 11 64 位，源码需要 Python 3.10+ 与 Tcl/Tk。
- 构建工作流使用 Python 3.12 和固定版本 PyInstaller；未提供代码签名。
- 增强后继续编辑会改变原生 Ctrl+Z 的撤销对象；准确恢复可从历史复制原文。目标应用兼容性请先在普通输入框测试。

## [1.0.2]

- 保留原有桌面托盘、全局快捷键、双模板增强、本地历史，以及 CLI、代理和 MCP 入口。
- 该版本的详细变更未在仓库中形成可核验的发布记录。
